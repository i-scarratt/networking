name: arista-dual

topology:
  nodes:
    switch1:
      kind: ceos
      image: ceos:latest # Ensure this matches the tag you used during 'docker import'
    switch2:
      kind: ceos
      image: ceos:latest

  links:
    - endpoints: ["switch1:eth1", "switch2:eth1"]
